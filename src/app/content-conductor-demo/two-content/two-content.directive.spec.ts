import { TwoContentDirective } from './two-content.directive';

describe('TwoContentDirective', () => {
  it('should create an instance', () => {
    const directive = new TwoContentDirective();
    expect(directive).toBeTruthy();
  });
});
