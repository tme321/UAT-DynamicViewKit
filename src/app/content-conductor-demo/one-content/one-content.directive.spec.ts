import { OneContentDirective } from './one-content.directive';

describe('OneContentDirective', () => {
  it('should create an instance', () => {
    const directive = new OneContentDirective(null);
    expect(directive).toBeTruthy();
  });
});
