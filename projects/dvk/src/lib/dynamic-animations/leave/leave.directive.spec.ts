import { LeaveDirective } from './leave.directive';

describe('LeaveDirective', () => {
  it('should create an instance', () => {
    const directive = new LeaveDirective();
    expect(directive).toBeTruthy();
  });
});
